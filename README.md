# Ada Bubbles - Digital Muse Platform

## 🌸 О проекте

Персональная веб-платформа для цифровой модели Ada Bubbles.

---

## 📁 Структура проекта (актуальная)

```
AdaBubbles/
├── package.json
├── server/
│   └── app.js              ← Сервер (обнови из server-app-updated.js)
└── public/
    ├── index.html          ← Главная страница
    ├── admin.html          ← 👑 АДМИНКА (новое!)
    ├── 404.html            
    ├── robots.txt          
    ├── sitemap.xml         
    ├── _redirects          
    ├── assets/
    │   └── images/
    │       ├── avatar.jpg
    │       └── content/
    └── games/
        └── index.html
```

---

## 👑 Админ-панель

### Доступ
- URL: `https://adabubbles.onrender.com/admin.html`
- Логин: `ada`
- Пароль: `bubbles2025`

### Возможности
- 📊 **Dashboard** — статистика (пользователи, сообщения, посты)
- 👥 **Users** — список зарегистрированных пользователей, удаление
- 💬 **Messages** — просмотр и удаление сообщений от пользователей
- 📝 **Content** — добавление/редактирование/удаление постов
- ⚙️ **Settings** — смена пароля админа

### Как обновить сервер

1. Скопируй содержимое `server-app-updated.js`
2. Замени им файл `server/app.js` в репозитории
3. Закоммить и запушь:
```bash
git add .
git commit -m "Add admin panel"
git push
```
4. Render автоматически пересоберёт проект

---

## 🚀 Быстрый старт (локальный запуск)

```bash
# Клонируй репозиторий
git clone https://github.com/YOUR_USERNAME/AdaBubbles.git
cd AdaBubbles

# Установи зависимости
npm install

# Запусти сервер
npm start

# Открой в браузере
# Сайт:    http://localhost:3000
# Админка: http://localhost:3000/admin.html
```

---

## 🌐 Деплой на Render

### Уже настроено:
- URL: https://adabubbles.onrender.com
- Автодеплой при пуше в main

### Переменные окружения (опционально)
В настройках Render можно добавить:
- `ADMIN_USERNAME` — логин админа (по умолчанию `ada`)
- `ADMIN_PASSWORD` — пароль админа (по умолчанию `bubbles2025`)

---

## 🔌 API Эндпоинты

### Публичные (без авторизации)

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/register` | Регистрация пользователя |
| POST | `/api/message` | Отправка сообщения |
| GET | `/api/messages` | Получение сообщений |
| GET | `/api/posts` | Получение постов |

### Админские (нужен токен)

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/admin/login` | Авторизация админа |
| GET | `/api/admin/verify` | Проверка токена |
| GET | `/api/admin/stats` | Статистика |
| GET | `/api/admin/users` | Список пользователей |
| DELETE | `/api/admin/users/:id` | Удалить пользователя |
| GET | `/api/admin/messages` | Все сообщения |
| DELETE | `/api/admin/messages/:index` | Удалить сообщение |
| DELETE | `/api/admin/messages` | Удалить все сообщения |
| GET | `/api/admin/posts` | Все посты |
| POST | `/api/admin/posts` | Создать пост |
| PATCH | `/api/admin/posts/:id` | Редактировать пост |
| DELETE | `/api/admin/posts/:id` | Удалить пост |

---

## 🖼️ Добавление изображений

### Аватар Ada
В `index.html` найди:
```html
<span class="avatar-placeholder">AB</span>
```
Замени на:
```html
<img src="assets/images/avatar.jpg" alt="Ada" style="width:100%;height:100%;object-fit:cover;">
```

### Посты
Найди:
```html
<div class="card-image-placeholder">🌸</div>
```
Замени на:
```html
<img src="assets/images/content/post1.jpg" alt="Post" style="width:100%;height:100%;object-fit:cover;">
```

---

## 💳 Интеграция платежей (следующий этап)

### Варианты:
- **Stripe** — карты (нужен бизнес)
- **PayPal** — проще для старта
- **Crypto** — Coinbase Commerce / NOWPayments

### Для крипто (самое простое):
1. Создай аккаунт на https://nowpayments.io
2. Получи API ключ
3. Добавь кнопку оплаты

---

## 🔐 Безопасность

⚠️ **Важно для продакшена:**

1. **Смени пароль админа** через Settings в админке
2. **Хэшируй пароли** — добавь bcrypt для паролей пользователей
3. **Используй базу данных** — MongoDB Atlas (бесплатно) или PostgreSQL
4. **HTTPS** — Render предоставляет автоматически

---

## 📱 Контакты

- Website: https://adabubbles.onrender.com
- GitHub: https://github.com/YOUR_USERNAME/AdaBubbles

---

## ✅ Чек-лист запуска

- [x] Сервер на Render
- [x] Welcome-экран (язык + 18+)
- [x] Регистрация/авторизация
- [x] Чат с Адой
- [x] Админ-панель
- [ ] Реальные фотографии
- [ ] Интеграция платежей
- [ ] База данных (MongoDB)
- [ ] Кастомный домен
